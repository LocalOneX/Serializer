Simple and lightweight ROBLOX Luau Serializer, this will basically turn **most** inputs into a copyable string.

Basic usage:
```lua
	local Serializer = require(path_to_serializer)

	Serializer({
		test = {
			test = {
				test = {"test", "test", "test"_
			}
		}
	})
```
